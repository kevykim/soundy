type id = {
    id : number
}

type userId = {
    userId : number
}

interface AlbumInterface {
    title : string
    description : string
    year : number
    imageUrl : string
    id
    userId
}